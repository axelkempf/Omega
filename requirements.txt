# =============================================================================
# requirements.txt - Thin Wrapper (Single Source of Truth: pyproject.toml)
# =============================================================================
#
# This file exists for backward compatibility and CI tooling that requires
# a requirements.txt file (e.g., pip-audit, Dependabot).
#
# All dependencies are defined in pyproject.toml. This file simply references
# the package with all optional extras installed.
#
# For development setup, prefer:
#   pip install -e .[dev,analysis]
#
# For full environment (dev + analysis + ml):
#   pip install -e .[all]
#
# =============================================================================

# Install the package itself with dev and analysis extras
-e .[dev,analysis]

